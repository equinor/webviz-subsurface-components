name: Semantic Release

on:
  push:
    branches:
      - 'master'

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          persist-credentials: false

      - name: configure git
        run: |
          git config user.name "hkfb"
          git config user.email "hkfb@users.noreply.github.com"

      - run: npm ci
        working-directory: ./typescript

      - name: Run semantic release
        working-directory: ./typescript
        run: npx nx run-many --target=semantic-release --parallel=false
        env:
          GITHUB_TOKEN: ${{ secrets.SEMANTIC_RELEASE_GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          GIT_AUTHOR_NAME: "hkfb"
          GIT_AUTHOR_EMAIL: "hkfb@users.noreply.github.com"
          GIT_COMMITTER_NAME: "hkfb"
          GIT_COMMITTER_EMAIL: "hkfb@users.noreply.github.com"