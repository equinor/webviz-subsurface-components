// Shader
import shaderVertex from '../shaders/vertexShader.vs.glsl';
import shaderFragment from '../shaders/fragmentShader.fs';

// Utils
import { loadImage, createProgram, createShader, bindBuffer, bindTexture } from './webglutils';

export default (canvas, image, colormap) => {
    const gl = canvas.getContext("webgl");

    



}